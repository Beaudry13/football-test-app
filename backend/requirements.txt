Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-JWT-Extended==4.6.0
Flask-Bcrypt==1.0.1
Flask-Cors==4.0.1
Flask-Limiter==4.1.1
psycopg2-binary==2.9.9
marshmallow==3.21.3
python-dotenv==1.0.1
gunicorn==22.0.0
reportlab==5.0.0
boto3==1.43.62
Pillow==12.3.0

pytest==8.2.2
pytest-flask==1.3.0
pypdf==6.14.2
# Rasterises PDF pages and exposes the text layer with bounding boxes.
# Apache-2.0/BSD - deliberately not PyMuPDF, whose AGPL is a hazard for a
# hosted product. See docs/DESIGN-playbook-quiz.md §3.
pypdfium2==5.12.1
# Authenticated encryption (Fernet) for the ONE reversible secret in Peira: a
# pending coach invitation's token, so the owner can reshare the same code
# later. Redemption still matches a SHA-256; see services/invite_secrets.py.
cryptography==43.0.3
